var story = document.querySelectorAll('.story');
console.log(story)
 
story.addEventListener('click', storyDisappear)
 
function storyDisappear() {
 if (storyDisappear = story.addEventListener) {
     story = story.style.red
 }
}
